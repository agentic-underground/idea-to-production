# ── DESTINATION: clippy.toml (repo root) ─────────────────────────────────────
# GOVERNING DOC: knowledge/07-quality-gate-and-process.md §1
# Project-level clippy configuration. The CI step enforces `-D warnings` on top,
# which is what actually makes lints blocking. Add threshold tweaks here as the
# project grows; keep the gate strict.
# ─────────────────────────────────────────────────────────────────────────────
# (Intentionally minimal. The -D warnings gate does the heavy lifting.)
