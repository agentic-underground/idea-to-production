# Add the publish audio-script target (format=audio-script)

## Metadata
| | |
| --- | --- |
| **Plan** | `0021` |
| **Epic** | `0018` |
| **Depends on** | `PLAN_0019` _(extends the `format=` dispatch surface PLAN_0019 establishes)_ |

**Brief**: Promote the writer's existing TTS hinting (flowing prose, no inline headers, em-dash pacing) to
a first-class `format=audio-script` target for narration pipelines — output shaped to be read aloud rather
than scanned on a page.

## User stories
- AS a creator I WANT an article rendered as a narration script SO THAT I can feed it to a TTS pipeline
  without hand-stripping headers and list syntax.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL produce a narration-optimised script
  (`format=audio-script`): flowing prose, no inline markdown headers/list markers, paced for speech.
- `EARS-002` (Event-driven): WHEN `format=audio-script` is requested THE SYSTEM SHALL transform headers
  into spoken transitions, expand or drop figure references that cannot be read aloud, and normalise
  punctuation for pacing.
- `EARS-003` (Unwanted): IF the source contains content that cannot be voiced (a raw table, a code block)
  THEN THE SYSTEM SHALL summarise or annotate it for narration rather than emit literal markup into the
  script.

## Acceptance criteria
1. Given an article, When `format=audio-script` runs, Then the output contains no markdown header/list
   syntax and reads as continuous prose.
2. Given a figure reference, When the script is produced, Then it is rendered as a spoken description or
   omitted, never left as a raw image link.

## Human interface test plan
None — `format=audio-script` is a skill invocation. Proof: the output is asserted to contain no header/list
markup and no raw image links (continuous prose), validated on the rendered script.

## Implementation notes
- Build on the writer skill's existing audio-script hinting; add the explicit `format=audio-script`
  target into PLAN_0019's `format=` dispatch.
- Define the header→spoken-transition and figure→narration transforms, and the table/code-block→summary
  handling.
- **Rename caveat:** target the current `publish` plugin (legacy text says "PRESSROOM").

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0018, PLAN_0021)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (no header/list markup; figure→spoken description;
table/code→summary not literal markup), the test coordinates, then the `format=audio-script` transform.
Emit `STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT
`git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue,
and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; the non-voiceable-content path summarises rather than emits literal
  markup; coverage of the transform is 100%.
