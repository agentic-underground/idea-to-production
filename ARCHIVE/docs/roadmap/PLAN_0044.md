# Grow the element registry (new registry elements)

## Metadata
| | |
| --- | --- |
| **Plan** | `0044` |
| **Epic** | `0039` |
| **Depends on** | — _(the `data-table` candidate consumes PLAN_0040's virtualization discipline — see EPIC_0039 shared-infra)_ |

**Brief**: Grow the element registry with new candidate elements — data-table (editable, sortable,
virtualized), date/time picker, relationship explorer, hierarchical/tree selector, rich-text editor, file
uploader, map/geographic picker, levers/sliders, and a chart family. Each ships as a registry page in the
established anatomy and is added to `elements/README.md` so `-help` stays current. This is the umbrella for
registry growth; each element is its own incremental deliverable.

## User stories
- AS a builder I WANT more ready-made registry elements SO THAT I can compose richer data-bound UIs without
  authoring each control from scratch.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL grow the element registry with new elements, each following
  the established registry-page anatomy and INTENT-marker conventions.
- `EARS-002` (Event-driven): WHEN a new element is added THE SYSTEM SHALL register it as a registry page
  and add it to `elements/README.md` so `-help` lists it.
- `EARS-003` (State-driven): WHILE an element is in the registry THE SYSTEM SHALL keep it discoverable via
  `-help` and consistent with the design system's accessibility/dark-mode/keyboard rules.

## Acceptance criteria
1. Given a new element, When added, Then it has a registry page in the standard anatomy and appears in
   `-help`.
2. Given any registry element, When inspected, Then it meets the design-system accessibility/keyboard
   rules.

## Human interface test plan
- **[new element registration]** add a new element (e.g. data-table) → its registry page follows the
  standard anatomy → `-help` lists it → exercise it in a browser-level story for keyboard/accessibility
  conformance (roving focus, dark mode, INTENT markers).

## Implementation notes
- Candidate set (each its own incremental deliverable): data-table, date/time picker, relationship
  explorer, tree selector, rich-text editor, file uploader, map picker, levers/sliders, chart family.
- The **`data-table`** element consumes PLAN_0040's windowing/focus discipline (the shared-infra link).
- Each element follows the registry-page anatomy and is added to `elements/README.md` so `-help` stays
  current — see `plugins/foundry/skills/frontend/`. As an umbrella, this PLAN may spawn a sub-slice per
  element; the slice here establishes the growth pattern + at least one new element end-to-end.

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0039, PLAN_0044)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (new element has a standard registry page + appears
in `-help`; any element meets a11y/keyboard rules), the browser-level story coordinates, then the
registry-growth pattern + at least one new element end-to-end (and its `elements/README.md` entry). Emit
`STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT
`git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue,
and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (J four-mirror if a new skill surface is added,
  P) stays GREEN.
- Both acceptance criteria pass; the new element is discoverable via `-help` and meets the a11y/keyboard/
  dark-mode rules; coverage of the new element + registration is 100%.
