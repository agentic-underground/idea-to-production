# Add the license-audit lens (SPDX policy + copyleft conflicts)

## Metadata
| | |
| --- | --- |
| **Plan** | `0032` |
| **Epic** | `0029` |
| **Depends on** | — |

**Brief**: Scan dependency licences for compatibility and copyleft conflicts (e.g. GPL in a permissively-
licensed product). Map each dependency's SPDX licence against a configurable policy; flag conflicts and
unknown/missing licences. Pairs with `scan-dependencies` (same manifests, different question).

## User stories
- AS a maintainer shipping a permissively-licensed product I WANT to know if a dependency's licence
  conflicts with my policy SO THAT I don't unknowingly inherit copyleft obligations.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL resolve each dependency's SPDX licence and evaluate it against
  a configurable policy.
- `EARS-002` (Event-driven): WHEN `/security:license-audit` runs THE SYSTEM SHALL report each dependency's
  licence, flag policy conflicts (e.g. copyleft in a permissive product), and flag unknown/missing
  licences.
- `EARS-003` (Unwanted): IF a dependency's licence cannot be determined THEN THE SYSTEM SHALL flag it as
  unknown (a finding), and SHALL NEVER assume it is compatible.

## Acceptance criteria
1. Given a dependency graph and a policy, When the audit runs, Then every conflicting and every
   unknown-licence dependency is reported with its SPDX id.
2. Given a clean graph, When run, Then the audit passes with each licence listed.

## Human interface test plan
None — the audit is a scan invocation. Proof: a fixture graph with a copyleft dep under a permissive
policy reports the conflict with its SPDX id; an undeterminable licence is flagged unknown (asserted on
output).

## Implementation notes
- New lens/skill `plugins/security/skills/license-audit/`; reuse `scan-dependencies`' ecosystem/manifest
  detection. SPDX resolution per ecosystem; configurable allowed/denied SPDX-id policy.
- Fold the lens into the gate verdict; read exclusions from `.securityignore` (PLAN_0030).
- A new skill must satisfy `scripts/verify-prereqs.sh` check J (SKILL.md frontmatter; README names a
  /command) and any new probe in the `check` manifest must be no-download.
- **Rename caveat:** target the current `security` plugin (legacy text says SENTINEL).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0029, PLAN_0032)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (conflict reported with SPDX id; clean graph
passes; undeterminable → unknown finding), the test coordinates, then the license-audit lens + SPDX
resolution + policy evaluation + gate fold-in. Emit `STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch
HEAD** and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit `.pipeline.md` / the EPIC's `## Plans`
table** — the engine owns sync, land, PR/issue, and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (J for the new skill, B/G for new probes, P)
  stays GREEN.
- Both acceptance criteria pass; undeterminable licences are flagged unknown (never assumed compatible);
  coverage of the licence-resolution + policy logic is 100%.
