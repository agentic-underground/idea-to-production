# Build the comfyui-mcp server/ test-first (5 MCP tools)

## Metadata
| | |
| --- | --- |
| **Plan** | `0046` |
| **Epic** | `0045` |
| **Depends on** | — |

**Brief**: FOUNDRY consumes `comfyui-mcp/EARS.md` and builds `server/` test-first to the 100% line+branch
coverage floor — the five MCP tools (`list_models`, `set_model`, `list_templates`, `submit_prompt`,
`get_result`), the allowlist loaders, the param validators, and the `/view` proxy. This is the buildable
core the enforcement (PLAN_0047) and hardening (PLAN_0048) layers refine.

## User stories
- AS a builder I WANT the comfyui-mcp server built test-first to the coverage floor SO THAT its security
  enforcement is pinned by tests, not asserted in prose.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL implement the five MCP tools (`list_models`, `set_model`,
  `list_templates`, `submit_prompt`, `get_result`) with the allowlist loaders, param validators, and
  `/view` proxy.
- `EARS-002` (Event-driven): WHEN `submit_prompt` is called THE SYSTEM SHALL accept a `template_id` (not a
  node graph), validate params, submit to ComfyUI, and return a handle pollable via `get_result`.

## Acceptance criteria
1. Given `comfyui-mcp/EARS.md`, When `server/` is built, Then the five tools exist with the allowlist
   loaders + param validators + `/view` proxy, at the 100% line+branch coverage floor.
2. Given the test suite, When run, Then it passes and pins each tool's happy + refusal paths.

## Human interface test plan
None — the MCP tools are a programmatic verb surface, not a UI element. Proof: the test suite exercises
each tool's happy + refusal path against a stubbed ComfyUI, at the 100% coverage floor.

## Implementation notes
- Test-first per the FOUNDRY DEV_SYSTEM; source the spec in `comfyui-mcp/EARS.md`. The enforcement
  specifics (allowlist semantics, validation bounds) are detailed in PLAN_0047/PLAN_0048 — this PLAN builds
  the buildable core they refine.
- ComfyUI is stubbed/mocked in tests (no live backend) so the suite is deterministic (the FLAKY-TEST ban).
- When PLAN_0050 registers the server, the `.mcp.json` launcher must be version-pinned (check K) — keep
  that in mind so the core is launchable that way.

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0045, PLAN_0046)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the happy/refusal Gherkin per tool, the test coordinates
(stubbed ComfyUI), then the `server/` (five tools + allowlist loaders + param validators + `/view`
proxy) to the 100% line+branch floor. Emit `STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD**
and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** —
the engine owns sync, land, PR/issue, and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; each tool's happy + refusal path is pinned; coverage is 100% line+branch
  (ComfyUI stubbed — no flakiness).
