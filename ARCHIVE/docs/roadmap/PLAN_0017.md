# Complete the two-form documentation split (per-plugin audience tagging + index)

## Metadata
| | |
| --- | --- |
| **Plan** | `0017` |
| **Epic** | `0015` |
| **Depends on** | — |

**Brief**: Finish the user-facing / agent-facing documentation split started by the `docs/` reorg. Tag
per-plugin docs by audience and generate an audience index, so the two-form convention extends beyond the
top-level `docs/` tree into each plugin.

## User stories
- AS a human reader I WANT to find user-facing docs without wading through agent-facing material SO THAT
  onboarding is clean.
- AS an agent I WANT agent-facing docs clearly marked SO THAT I load the right context.

## EARS specification
- `EARS-001` (Ubiquitous): EACH substantial doc SHALL declare its audience (`user` | `agent`)
  discoverably (frontmatter `audience:` or location).
- `EARS-002` (Event-driven): WHEN the audience index is generated THE SYSTEM SHALL list docs grouped by
  audience across the repo.

## Acceptance criteria
1. Per-plugin docs carry an audience signal (frontmatter `audience:` or placement).
2. A generated index groups docs by audience.
3. The top-level `docs/guide` vs `docs/internal` split is consistent with the per-plugin tagging.

## Human interface test plan
None — this slice produces documentation metadata + a generated index; it adds no user-facing UI element.
The proof is that the index correctly partitions docs by their declared audience signal.

## Implementation notes
- **Builds on** the `docs/guide` / `docs/internal` / `docs/historical` split already in place — extend
  the same audience convention into each plugin's docs.
- **Keep README / CLAUDE / KAIZEN at root** — these canons are injected (SessionStart) and CI-checked for
  byte-identical mirroring; they must NOT move.
- The audience signal is a frontmatter `audience:` key (or discoverable placement); the index is
  generated (not hand-maintained) so it cannot drift from the tagging.
- The generated index must pass `scripts/verify-prereqs.sh` check I (every repo-local doc link in it
  resolves on disk).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0015, PLAN_0017)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — define the `audience:` signal, tag the substantial per-plugin docs, author the
index generator + its output, and pin the behaviour with tests (a doc with no audience is flagged; the
index groups by audience; the top-level split stays consistent). Emit `STORY_PROVEN` +
`DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit
`.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue, and the `state`
column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (incl. I doc-link resolution over the generated
  index and P roadmap conformance) stays GREEN.
- Per-plugin docs carry an audience signal; the generated index groups docs by audience; the
  `docs/guide` ⟷ `docs/internal` split is consistent with the per-plugin tagging; the index generator's
  behaviour is pinned by tests.
