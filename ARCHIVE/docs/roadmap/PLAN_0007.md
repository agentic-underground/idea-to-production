# Migrate the frontend / atelier backlog to v2

## Metadata
| | |
| --- | --- |
| **Plan** | `0007` |
| **Epic** | `0003` |
| **Depends on** | — |

**Brief**: decompose the frontend / atelier design-system legacy backlog items into v2 EPIC/PLAN docs.

## Legacy items in this batch
`.i2p/roadmap/backlog/`: 68 (large-dataset virtualization), 69 (large-graph canvases), 70 (serverless
device-swap), 71 (automate design-critic spawning), 72 (element-registry growth).

## Construction process
Run roadmapper's **authoring value task** (§3.5): carve these into v2 docs under a "frontend design-system
growth" EPIC (they share the `foundry:frontend` / atelier design-system surface). Use
`handler-roadmap-decomposition` for INVEST slices + dependency order. Preserve each item's EARS/acceptance
into the `PLAN_NNNN.md` sections. Append the new EPIC row(s) to `.pipeline.md`; claim global ids from the
shared counter. No push; keep legacy files.

## Definition of done
- Each listed legacy item is represented by a conformant v2 doc (mapping recorded here on completion).
- `scripts/verify-prereqs.sh` check P GREEN; `.pipeline/verify` passes.

## Migration mapping (completed)
The five frontend/atelier (legacy "FRONTEND") backlog items 68–72 are migrated into one conformant v2
EPIC under `docs/roadmap/` — [EPIC_0039.md](./EPIC_0039.md) ("grow the frontend design system") — ids
claimed from the shared counter (`0039`–`0044`, after PLAN_0006's `0029`–`0038`), with the EPIC row
appended to the `.pipeline.md` manifest (state `available`):

| legacy item | v2 doc |
| --- | --- |
| 68 (large-dataset virtualization) | [PLAN_0040.md](./PLAN_0040.md) |
| 69 (large graph-canvases) | [PLAN_0041.md](./PLAN_0041.md) |
| 70 (serverless device-swap) | [PLAN_0042.md](./PLAN_0042.md) |
| 71 (automate design-critic spawning) | [PLAN_0043.md](./PLAN_0043.md) |
| 72 (element-registry growth) | [PLAN_0044.md](./PLAN_0044.md) |

Ids `0039`–`0044` consumed; manifest row `0039` appended. All five PLANs share the `foundry:frontend`
design-system surface (registry-page anatomy, INTENT markers, accessibility/keyboard/dark-mode rules).

**Provenance notes:**
- These items target the **current** `foundry:frontend` design system (`plugins/foundry/skills/frontend/`)
  and the `atelier` design-critic — no plugin rename was needed (unlike PLAN_0005's pressroom→publish or
  PLAN_0006's SENTINEL→security).
- **PLAN_0042 (serverless device-swap) is research-grade/strategic** — its Construction process begins
  with a transport-choice spike and may spawn follow-on sub-slices under EPIC_0039; **PLAN_0044
  (registry growth) is an umbrella** that establishes the growth pattern + ≥1 new element end-to-end and
  may likewise spawn a sub-slice per element. The `data-table` candidate consumes PLAN_0040's
  virtualization discipline (shared infra).
- Legacy `.i2p/roadmap/backlog/*` files are **kept as history** (not deleted), per the migration decision.
<!-- pipeline-tokens:start -->
> **Pipeline tokens:** plan 0003.0003 — $5.4561.
<!-- pipeline-tokens:end -->
