# Add code-quality ADR integration

## Metadata
| | |
| --- | --- |
| **Plan** | `0059` |
| **Epic** | `0054` |
| **Depends on** | — |

**Brief**: Detect existing Architecture Decision Records in `docs/decisions/`, cross-reference findings
against recorded decisions, and suggest new ADRs when a pattern inconsistency is found.

## User stories
- AS an architect I WANT code-quality to check findings against our recorded ADRs SO THAT it doesn't flag
  deliberate decisions, and suggests an ADR when it finds an undocumented inconsistency.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL detect ADRs in `docs/decisions/` and cross-reference findings
  against them.
- `EARS-002` (Event-driven): WHEN a finding contradicts a recorded ADR THE SYSTEM SHALL note the ADR; WHEN
  a pattern inconsistency has no ADR THE SYSTEM SHALL suggest writing one.

## Acceptance criteria
1. Given an ADR that sanctions a pattern, When that pattern is found, Then the finding references the ADR
   rather than flagging it as a defect.
2. Given an undocumented inconsistency, When found, Then a new-ADR suggestion is emitted.

## Human interface test plan
None — ADR cross-referencing is analysis logic. Proof: a fixture `docs/decisions/` ADR that sanctions a
pattern turns a would-be finding into an ADR reference; an undocumented inconsistency emits a new-ADR
suggestion (asserted on the findings set).

## Implementation notes
- Parse `docs/decisions/` ADRs; map findings ↔ decisions; suggest ADRs for unrecorded inconsistencies. A
  sanctioned pattern must reference its ADR rather than flag a defect (avoids false positives on
  deliberate decisions).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0054, PLAN_0059)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (sanctioned pattern → ADR reference not defect;
undocumented inconsistency → new-ADR suggestion), the test coordinates, then the ADR parse +
cross-reference + suggestion logic. Emit `STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD** and
write `IDEA_COST.jsonl`. **Do NOT `git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** — the
engine owns sync, land, PR/issue, and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; sanctioned patterns reference their ADR (no false defect); coverage of the
  ADR-integration logic is 100%.
