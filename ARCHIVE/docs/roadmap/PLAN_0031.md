# Add the severity-policy config (security.config.json verdict tuning)

## Metadata
| | |
| --- | --- |
| **Plan** | `0031` |
| **Epic** | `0029` |
| **Depends on** | — |

**Brief**: A `security.config.json` letting a project tune the verdict rule — e.g. treat HIGH as BLOCK for
regulated products, or downgrade a specific advisory with a documented justification + expiry. Shared
infrastructure the gate applies; lands early.

## User stories
- AS a security owner of a regulated product I WANT to make HIGH findings BLOCK SO THAT the gate matches my
  risk posture, AND I WANT to downgrade a specific advisory with a justification + expiry SO THAT accepted
  risks are explicit and time-bounded.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL apply a project `security.config.json` to compute the gate
  verdict from findings.
- `EARS-002` (Event-driven): WHEN the security gate computes a verdict THE SYSTEM SHALL apply the
  configured severity→verdict mapping and any per-advisory downgrade that is in date.
- `EARS-003` (Unwanted): IF a per-advisory downgrade has passed its `expiry` THEN THE SYSTEM SHALL ignore
  the downgrade (the advisory returns to its default severity) and note the lapse.

## Acceptance criteria
1. Given a config mapping HIGH→BLOCK, When a HIGH finding exists, Then the gate verdict is BLOCK.
2. Given a downgrade for advisory X with a future expiry, When X is found, Then it is downgraded; given a
   past expiry, Then it is not (and the lapse is noted).

## Human interface test plan
None — `security.config.json` is read during verdict synthesis. Proof: a fixture config maps HIGH→BLOCK
and a HIGH finding yields BLOCK; an in-date downgrade applies, an expired one does not (asserted on the
computed verdict).

## Implementation notes
- Define `security.config.json` (severity→verdict map; per-advisory downgrades with justification +
  expiry). **Rename caveat:** legacy item names it `sentinel.config.json`; use `security.config.json`.
- Apply in `/security:scan-all`'s verdict synthesis (legacy `/security-gate`).
- Date handling must be deterministic in tests (inject the comparison time; no wall-clock flakiness).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0029, PLAN_0031)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (HIGH→BLOCK; in-date vs expired downgrade), the
test coordinates (injected clock — deterministic), then the config schema + verdict-synthesis application.
Emit `STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT
`git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue,
and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; expired downgrades revert to default severity with a noted lapse;
  coverage of the verdict-synthesis logic is 100% (deterministic clock — no flakiness).
