# Add publish web-publish targets (dev.to / Hashnode / static-site)

## Metadata
| | |
| --- | --- |
| **Plan** | `0022` |
| **Epic** | `0018` |
| **Depends on** | — |

**Brief**: Push finished articles to dev.to, Hashnode, or a static-site repo — front-matter generation,
canonical URLs, cover-image hookup — with a dry-run preview before anything is published.

## User stories
- AS a writer I WANT to publish a finished article to my blog platform from publish SO THAT I don't
  re-format front-matter and re-upload images by hand.
- AS a cautious publisher I WANT a dry-run preview SO THAT I see exactly what will be posted before it
  goes live.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL publish a finished article to a configured web target (dev.to,
  Hashnode, or a static-site repo), generating the target's front-matter and wiring the canonical URL +
  cover image.
- `EARS-002` (Event-driven): WHEN a publish is requested THE SYSTEM SHALL first produce a dry-run preview
  of the exact payload and require confirmation before the live post.
- `EARS-003` (Unwanted): IF the target API token is missing or invalid THEN THE SYSTEM SHALL stop with a
  clear message and publish nothing.

## Acceptance criteria
1. Given a finished article and a configured target, When publish runs, Then a dry-run preview is shown and
   the live post happens only after confirmation.
2. Given a published article, When inspected on the target, Then its front-matter, canonical URL, and cover
   image match the source.

## Human interface test plan
None — the dry-run/confirm flow is a CLI interaction, not a web UI element. Proof: the dry-run renders the
exact payload and no live call is made without confirmation (asserted against a mocked target adapter); a
missing/invalid token stops with a clear message and publishes nothing.

## Implementation notes
- Per-target **adapters** (dev.to / Hashnode APIs; static-site = commit to a repo). The dry-run preview is
  mandatory before any live call.
- **Tokens are runtime secrets** — never committed; the abuse-path test asserts a missing/invalid token
  publishes nothing. Keep `scripts/verify-prereqs.sh` secret-hygiene expectations intact (no token in any
  tracked file).
- This is a destination, not a `format=` — it consumes a rendered article and posts it.
- **Rename caveat:** target the current `publish` plugin (legacy text says "PRESSROOM").

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0018, PLAN_0022)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (dry-run then confirm; published metadata matches
source; missing token publishes nothing), the test coordinates (against mocked adapters — no live network),
then the target adapters + dry-run/confirm gate. Emit `STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to
**branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit `.pipeline.md` / the EPIC's
`## Plans` table** — the engine owns sync, land, PR/issue, and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN; no secret is committed.
- Both acceptance criteria pass; the missing/invalid-token path publishes nothing with a clear message;
  coverage of the adapters + dry-run gate is 100% (live network mocked in tests — no flakiness).
