<!--
Board linkage (EPIC 0066 — the marketplace self-hosts its own value flow).
Every PR either declares its board item OR logs a trivial-work exemption.

  • Declare it — replace the placeholder below with the EPIC/PLAN issue number on
    project #4 (e.g. the PLAN this PR delivers). A `pipeline/NNNN-*` branch also counts.
  • Trivial work only (typo / formatting / one-line chore) — delete the Board line and
    write instead:   [no-board]: <one-line reason>

The pre-push gate (scripts/verify-board-linkage.sh) and the CI backstop check this.
See CLAUDE.md → BOARD LINKAGE.
-->

Board: #<!-- issue number, digits only, e.g. the PLAN this delivers -->

## Summary

<!-- What this PR does and why. -->

## Verification

<!-- Gates run (.pipeline/verify, verify-routing, verify-prereqs), tests, evidence. -->
