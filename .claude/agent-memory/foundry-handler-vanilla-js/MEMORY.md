# Memory index

- [Flow-canvas test harness (vitest)](project_flow-canvas-harness.md) — how the static/ vanilla-JS test harness is wired and the mandated run command
- [Vitest config discovery via CWD](feedback_vitest-config-cwd.md) — vitest only finds its config from CWD; use `npm --prefix … test`, not `npx --prefix … vitest --dir`
- [Flow-server item JSON contract](reference_flow-server-item-contract.md) — the shape GET /api/items returns and the deps[] extension the canvas relies on
