"""`board` — GitHub Project board integration for idea-to-production (EPIC 0072).

Slice 1 (PLAN 0072.014): lifecycle transitions + EPIC rollup. Pure decisions live in `board.core`;
the `gh` I/O shell lives in `board.gh`; the CLI is `python3 -m board`.
"""
