# Station contracts (summary)

The authoritative per-station IN/EXIT/OUT contracts live in the `value-station-handoff`
skill. This reference exists so `founder-method` is self-contained for quick lookup; if the
two ever diverge, `value-station-handoff` wins and the divergence is a finding to fix.

IDEA→VALIDATE→SPEC→DESIGN→SLICE→HARDEN→SHIP→LEARN, then LEARN feeds IDEA.
Handlers: founder, marketer, roadmapper, frontend, builder, (reviewer+security-auditor),
founder, marketer.
