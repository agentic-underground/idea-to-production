{
  "$schema": "https://json.schemastore.org/claude-code-plugin.json",
  "name": "concierge",
  "displayName": "CONCIERGE — Repo welcome & the idea-to-production status line",
  "version": "1.6.0",
  "description": "The ARRIVAL layer of the idea-to-production marketplace — it greets whoever opens a repo and offers the suite's status line. When a project ships a maintainer-authored .claude/welcome.md, a SessionStart hook injects it so the agent opens with a one-line greeting and a conversational decision tree of the handful of things people come there to do — operate the software, or evolve it — and routes the user to the right command, runbook, or downstream plugin. Smart-gated: it greets only on a vague/cold open and steps aside the moment the user states a concrete task, so it never nags. When a repo has no welcome yet, a second SessionStart hook makes a single, smart-gated, in-the-know offer to author one (pre-populated from the repo's CLAUDE.md/README — and, under an active idea-to-production lifecycle, the product's emergent artifacts — so it is tailored, not bland; re-offered on cold opens until accepted or declined, with a global opt-out). Under an active lifecycle the welcome is managed — stamped with its phase and auto-refreshed as the product emerges, so whoever opens the repo always meets a current front door. /concierge:define-welcome authors and refreshes that welcome by reading the repo. CONCIERGE also ships the idea-to-production STATUS LINE: /concierge:statusline turns on a rich two-line ANSI bar (context & rate-limit gauges, the product-lifecycle phase, and a ⚔ reviewer-catch tally), and on first activation it makes a single unobtrusive offer to enable it. The engine ships in the marketplace; welcome content stays repo-local, exactly like CLAUDE.md. Stands alone; lights up ideator and foundry as the 'evolve the software' destinations when they are installed.",
  "author": {
    "name": "whatbirdisthat",
    "email": "95787+whatbirdisthat@users.noreply.github.com",
    "url": "https://github.com/whatbirdisthat"
  },
  "homepage": "https://github.com/whatbirdisthat/idea-to-production/tree/main/plugins/concierge",
  "repository": "https://github.com/whatbirdisthat/idea-to-production",
  "license": "MIT OR Apache-2.0",
  "category": "workflow",
  "defaultEnabled": true,
  "keywords": [
    "welcome",
    "onboarding",
    "session-start",
    "developer-experience",
    "greeting",
    "decision-tree",
    "wayfinding",
    "hooks",
    "concierge",
    "repo-front-door",
    "statusline",
    "status-bar"
  ],
  "metadata": {
    "companions": {
      "refinement": "ideator",
      "carriage": "foundry"
    },
    "note": "skills/, commands/, agents/, and hooks/hooks.json are auto-discovered from the plugin root. /concierge:define-welcome authors a repo's .claude/welcome.md; the SessionStart hook (hooks/inject-welcome.sh) renders it when present (silent no-op when absent); a companion SessionStart hook (hooks/offer-welcome.sh) proactively offers to author one when absent and instructs a silent auto-refresh of a managed, lifecycle-stamped welcome when it falls out of step, then VERIFIES the re-stamp landed and discloses on mismatch (opt-out markers live under ~/.claude/hook-state; the hook never writes the repo). The status line ships in statusline/: /concierge:statusline runs statusline/install.sh (copies the version-stamped i2p-statusline.sh → ~/.claude, points settings.json at it); a PostToolUse hook (statusline/count-adversarial-catches.sh) feeds the ⚔ caught tally from the shared statusline/adversarial-artifacts.lst glob set; SessionStart hooks offer the status line once (hooks/offer-statusline.sh) and detect installed-vs-shipped renderer drift (hooks/check-statusline-drift.sh, offers /concierge:statusline to refresh). Token safety has moved OUT of CONCIERGE: the token-aware scheduler is now the standalone token-fairness marketplace plugin (the tested `tf` binary), installed separately (/plugin install scheduler@token-fairness); CONCIERGE no longer ships it. /concierge:check probes tool deps (skills/check/requirements.tsv) and /concierge:inspect runs the inspector agent. Carries the byte-identical KAIZEN.md + inject-kaizen.sh + check.sh + inspection-core.md canonical copies (CI verify-prereqs.sh checks A/N/O)."
  }
}
