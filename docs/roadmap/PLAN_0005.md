# Migrate the publish backlog to v2

## Metadata
| | |
| --- | --- |
| **Plan** | `0005` |
| **Epic** | `0003` |
| **Depends on** | — |

**Brief**: decompose the publish-plugin legacy backlog items into conformant v2 EPIC/PLAN docs.

## Legacy items in this batch
`.i2p/roadmap/backlog/`: 50 (slide-deck skill), 51 (multi-format export), 52 (TTS-optimised output),
53 (citations & bibliography), 54 (web publish targets), 55 (changelog/release-notes automation),
56 (brand-style themes), 57 (diagram round-tripping), 58 (localisation).

## Construction process
Run roadmapper's **authoring value task** (§3.5): carve these into v2 docs. The nine publish features
likely group under one or two EPICs (e.g. "publish output formats" + "publish authoring aids"); use
`handler-roadmap-decomposition` for the shared-infra map (they share the publish render pipeline) and
the dependency order. Preserve each item's EARS/acceptance into the `PLAN_NNNN.md` sections. Append the
new EPIC rows to `.pipeline.md`; claim global ids from the shared counter. No push; keep legacy files.

## Definition of done
- Each listed legacy item is represented by a conformant v2 doc (mapping recorded here on completion).
- `scripts/verify-prereqs.sh` check P GREEN; `.pipeline/verify` passes.

## Migration mapping (completed)
The nine publish (legacy "PRESSROOM") backlog items are migrated into two conformant v2 EPICs under
`docs/roadmap/` (ids claimed from the shared counter, `0018`–`0028`, after PLAN_0004's `0010`–`0017`),
with each EPIC row appended to the `.pipeline.md` manifest (state `available`):

| legacy item | v2 doc | EPIC |
| --- | --- | --- |
| 51 (multi-format export — docx/epub/html) | [PLAN_0019.md](./PLAN_0019.md) | EPIC_0018 |
| 50 (slide-deck skill) | [PLAN_0020.md](./PLAN_0020.md) | EPIC_0018 |
| 52 (TTS-optimised output — audio-script) | [PLAN_0021.md](./PLAN_0021.md) | EPIC_0018 |
| 54 (web-publish targets) | [PLAN_0022.md](./PLAN_0022.md) | EPIC_0018 |
| 53 (citations & bibliography) | [PLAN_0024.md](./PLAN_0024.md) | EPIC_0023 |
| 55 (changelog/release-notes automation) | [PLAN_0025.md](./PLAN_0025.md) | EPIC_0023 |
| 56 (brand/style themes) | [PLAN_0026.md](./PLAN_0026.md) | EPIC_0023 |
| 57 (diagram round-tripping) | [PLAN_0027.md](./PLAN_0027.md) | EPIC_0023 |
| 58 (localisation) | [PLAN_0028.md](./PLAN_0028.md) | EPIC_0023 |

Two EPICs emitted: [EPIC_0018.md](./EPIC_0018.md) (expand the publish output-format & channel matrix —
items 51/50/52/54; PLAN_0019 establishes the shared `format=` dispatch the others extend) and
[EPIC_0023.md](./EPIC_0023.md) (publish authoring & enrichment aids — items 53/55/56/57/58). Ids
`0018`–`0028` consumed; manifest rows `0018`/`0023` appended.

**Provenance notes:**
- **Rename pressroom → publish.** Every legacy item names the plugin "PRESSROOM" with paths
  `plugins/pressroom/` and `/pressroom:publish`; the plugin is now `publish` (`plugins/publish/`,
  `/publish:publish`). The PLANs instruct the builder to target the current `publish` plugin (and item
  56's config is renamed `pressroom.config.json` → `publish.config.json`).
- **Retired-`flow` re-target.** Item 55 suggested reusing the retired `flow-server`'s git-log synthesis;
  PLAN_0025 re-targets it onto plain `git log` (the `flow` plugin was retired in `da07db8`).
- Legacy `.i2p/roadmap/backlog/*` files are **kept as history** (not deleted), per the migration decision.
