# Add CI integration recipes (drop-in security-gate snippets)

## Metadata
| | |
| --- | --- |
| **Plan** | `0034` |
| **Epic** | `0029` |
| **Depends on** | `PLAN_0033` _(the SBOM-artefact upload step consumes the SBOM lens from PLAN_0033 — legacy item 63 `depends_on: #60`)_ |

**Brief**: Drop-in GitHub Actions / GitLab CI snippets that run the security gate and fail the build on
BLOCK, post the report as a PR comment, and upload the SBOM as an artefact.

## User stories
- AS a team I WANT a copy-paste CI snippet that runs the security gate and fails on BLOCK SO THAT security
  is enforced on every PR without me wiring it from scratch.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL provide drop-in CI recipes (GitHub Actions and GitLab CI) that
  run the security gate and enforce its verdict.
- `EARS-002` (Event-driven): WHEN the gate returns BLOCK in CI THE SYSTEM SHALL fail the job; WHEN it runs
  on a PR THE SYSTEM SHALL post the report as a comment and upload the SBOM as an artefact.
- `EARS-003` (Unwanted): IF the gate cannot run (missing tool) THEN the recipe SHALL fail loudly with the
  missing prerequisite, not pass vacuously.

## Acceptance criteria
1. Given the GitHub Actions recipe, When a BLOCK verdict occurs, Then the workflow fails and the report is
   posted as a PR comment.
2. Given a passing run, When complete, Then the SBOM artefact is uploaded.

## Human interface test plan
None — the recipes are CI config snippets. Proof: linting/dry-run of the GitHub Actions + GitLab CI
recipes (e.g. `actionlint`) confirms a BLOCK fails the job and the SBOM-upload + PR-comment steps are
wired; a missing-tool path fails loudly (asserted via the recipe's guard step).

## Implementation notes
- Ship `recipes/` snippets (GitHub Actions + GitLab CI). **Pin actions to full SHAs**, least-privilege
  explicit `permissions:` — consistent with the marketplace's own CI discipline (and check K's MCP-pin
  ethos). Validate with `actionlint`/dry-run where available.
- The SBOM-artefact-upload step consumes PLAN_0033's `/security:sbom`.
- **Rename caveat:** the gate is `/security:scan-all` (legacy `/security-gate`).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0029, PLAN_0034)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (BLOCK fails job + PR comment; pass uploads SBOM;
missing tool fails loudly), the test coordinates (recipe lint/dry-run), then the GitHub Actions + GitLab
CI recipe snippets. Emit `STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD** and write
`IDEA_COST.jsonl`. **Do NOT `git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** — the engine
owns sync, land, PR/issue, and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN; any shipped workflow pins
  actions to full SHAs with least-privilege permissions.
- Both acceptance criteria pass; the missing-tool path fails loudly (never a vacuous pass); the recipes
  lint/dry-run clean.
