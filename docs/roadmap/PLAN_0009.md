# Migrate the code-quality backlog to v2

## Metadata
| | |
| --- | --- |
| **Plan** | `0009` |
| **Epic** | `0003` |
| **Depends on** | — |

**Brief**: migrate the code-quality legacy backlog — features + an already-EPIC-shaped pipeline — to v2.

## Legacy items in this batch
`.i2p/roadmap/backlog/`: 81 (stack-aware analysis), 82 (pre-commit hook), 83 (trend tracking),
84 (PR-diff mode), 85 (ADR integration), 86 (description optimiser), and the multi-agent pipeline
umbrella 87 (EPIC) with children 88 (spec-reviewer agent), 89 (coverage-gate agent), 90 (architecture-
guardian agent), 91 (pipeline orchestrator).

## Construction process
Run roadmapper's **authoring value task** (§3.5): emit item 87 as an `EPIC_NNNN.md` whose `## Plans` are
88–91 (each carrying that agent's EARS/acceptance), and group the standalone features 81–86 under a
"code-quality capabilities" EPIC (or fold where coupled). Use `handler-roadmap-decomposition` for the
shared-infra map (the code-quality skill + reviewer agents) and dependency order. Preserve each item's
EARS/acceptance into the `PLAN_NNNN.md` sections. Append the EPIC rows to `.pipeline.md`; claim global
ids from the shared counter. No push; keep legacy files.

## Definition of done
- Each listed legacy item is represented by a conformant v2 doc (mapping recorded here on completion).
- `scripts/verify-prereqs.sh` check P GREEN; `.pipeline/verify` passes.

## Migration mapping (completed)
The code-quality backlog (items 81–91) is migrated into two conformant v2 EPICs under `docs/roadmap/` —
ids claimed from the shared counter (`0054`–`0065`, after PLAN_0008's `0045`–`0053`), with both EPIC rows
appended to the `.pipeline.md` manifest (state `available`):

**[EPIC_0054.md](./EPIC_0054.md) — extend the code-quality capabilities** (standalone features 81–86):

| legacy item | v2 doc |
| --- | --- |
| 81 (stack-aware analysis) | [PLAN_0055.md](./PLAN_0055.md) |
| 82 (pre-commit hook) | [PLAN_0056.md](./PLAN_0056.md) |
| 83 (trend tracking + regression alerts) | [PLAN_0057.md](./PLAN_0057.md) |
| 84 (PR / diff mode) | [PLAN_0058.md](./PLAN_0058.md) |
| 85 (ADR integration) | [PLAN_0059.md](./PLAN_0059.md) |
| 86 (self-improving description optimiser) | [PLAN_0060.md](./PLAN_0060.md) |

**[EPIC_0061.md](./EPIC_0061.md) — build the multi-agent code-quality pipeline** (item 87 umbrella + children):

| legacy item | v2 doc |
| --- | --- |
| 87 (v2.0 multi-agent pipeline — the legacy EPIC) | [EPIC_0061.md](./EPIC_0061.md) |
| 88 (spec-reviewer agent) | [PLAN_0062.md](./PLAN_0062.md) |
| 89 (coverage-gate agent) | [PLAN_0063.md](./PLAN_0063.md) |
| 90 (architecture-guardian agent) | [PLAN_0064.md](./PLAN_0064.md) |
| 91 (pipeline orchestrator) | [PLAN_0065.md](./PLAN_0065.md) |

Ids `0054`–`0065` consumed; manifest rows `0054` and `0061` appended.

**Provenance notes:**
- **No plugin rename** — these items target the current `foundry:code-quality` skill
  (`plugins/foundry/skills/code-quality/`); item 91's orchestrator mirrors the existing
  `/foundry:pr-review` fan-out-then-synthesise pattern. No retired-`flow` reference.
- **Dependency order preserved:** item 91 (`depends_on: #88,#89,#90`) → PLAN_0065 `depends_on`
  PLAN_0062/0063/0064; the three agents land before the orchestrator.
- Legacy `.i2p/roadmap/backlog/*` files are **kept as history** (not deleted), per the migration decision.

This is the final batch of EPIC_0003: all legacy `.i2p/roadmap/backlog/` items (32/34/35/43/44/46,
49/50–58, 59–67, 68–72, 73–80, 81–91) are now represented by conformant v2 docs across PLANs 0004–0009.
<!-- pipeline-tokens:start -->
> **Pipeline tokens:** plan 0003.0005 — $8.1654.
<!-- pipeline-tokens:end -->
