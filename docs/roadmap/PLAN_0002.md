# Add a plugin-manifest schema assertion to the CI gate

## Metadata
| | |
| --- | --- |
| **Plan** | `0002` |
| **Epic** | `0001` |

## Construction process
1. In scripts/ci/check_manifests.py (or a new helper), assert each plugin manifest has the required keys
   (name, source/version) — a deterministic, offline check.
2. Keep .pipeline/verify green. Do NOT modify the marketplace plugins or the flow-server/MCP surface.

## Definition of done
- The gate asserts manifest shape; `.pipeline/verify` passes.
