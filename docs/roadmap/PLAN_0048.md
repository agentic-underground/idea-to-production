# Harden the comfyui-mcp trust boundary (path-safe /view, net isolation, authn)

## Metadata
| | |
| --- | --- |
| **Plan** | `0048` |
| **Epic** | `0045` |
| **Depends on** | `PLAN_0046` |

**Brief**: The trust-boundary hardening — a **path-traversal-safe `/view`** (filename/subfolder
canonicalised and confined to the output dir), **network isolation** (ComfyUI on a private container
network, never exposed; only the MCP port published), and **authn** (a token on the MCP endpoint;
`.mcp.json` approval-gating is the second layer).

## User stories
- AS a security owner I WANT the result proxy confined to the output dir, ComfyUI unexposed, and the MCP
  endpoint authenticated SO THAT neither arbitrary files nor the raw ComfyUI API are reachable.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL serve `/view` only for canonicalised paths inside the output
  dir, expose only the authenticated MCP port, and keep ComfyUI on a private network.
- `EARS-002` (Unwanted): IF a `/view` request contains a path-traversal sequence THEN THE SYSTEM SHALL
  refuse it; IF a request reaches the MCP port without a valid token THEN THE SYSTEM SHALL reject it.

## Acceptance criteria
1. Given `/view?filename=../../etc/passwd`, When requested, Then it is refused (confined to the output dir).
2. Given a request without a valid token, When it hits the MCP endpoint, Then it is rejected; given a valid
   token, Then it is served.

## Human interface test plan
None — boundary hardening is server/transport logic. Proof: a path-traversal `/view` is refused; an
untokened request is rejected and a tokened one served (asserted on responses). The network-isolation half
is realised with the container (PLAN_0049) and proven there by a host port scan.

## Implementation notes
- Canonicalise + confine the `/view` filename/subfolder to the output dir; token-gate the MCP endpoint
  (the `.mcp.json` approval-gate is the second layer). Network isolation is realised together with the
  container (PLAN_0049).
- Refines PLAN_0046's `/view` proxy + endpoint; no secret/token committed (the token is runtime).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0045, PLAN_0048)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (traversal refused; untokened rejected, tokened
served), the test coordinates, then the path-canonicalisation + token-gating. Emit `STORY_PROVEN` +
`DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit
`.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue, and the `state`
column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN; no token/secret is committed.
- Both acceptance criteria pass; path traversal is refused and untokened access rejected; coverage of the
  `/view` canonicalisation + authn logic is 100%.
