# Add publish localisation (parallel-language editions)

## Metadata
| | |
| --- | --- |
| **Plan** | `0028` |
| **Epic** | `0023` |
| **Depends on** | — _(shares the figure label-externalisation convention with PLAN_0027 — see EPIC_0023 shared-infra)_ |

**Brief**: Produce parallel-language editions of an article, with diagrams whose labels are externalised
and translated — so each locale reads natively, prose and figures alike.

## User stories
- AS a publisher with a global audience I WANT translated editions of an article — text AND diagram labels
  — SO THAT each locale reads natively, not just the prose.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL produce per-locale editions of an article, translating both
  the prose and the externalised diagram labels.
- `EARS-002` (Event-driven): WHEN a locale edition is requested THE SYSTEM SHALL render the article and
  re-render each figure with its labels swapped for the locale's translations.
- `EARS-003` (Unwanted): IF a diagram's labels are not externalised (hard-coded in the figure) THEN THE
  SYSTEM SHALL report that the figure cannot be localised rather than ship an untranslated figure
  silently.

## Acceptance criteria
1. Given an article and a target locale with translations, When the edition is built, Then both the prose
   and the figure labels appear in that locale.
2. Given a figure with non-externalised labels, When localising, Then the limitation is disclosed.

## Human interface test plan
None — localisation is a render-pipeline operation. Proof: a fixture article + locale translations builds
an edition with localised prose AND figure labels; a figure with hard-coded labels is reported as
non-localisable (asserted on build output).

## Implementation notes
- Establish a **label-externalisation convention** for charting-matrix figures (a labels map per figure) —
  this is the shared piece PLAN_0027 (round-tripping) should respect; whichever lands first owns it.
- **Translation source is provided per locale** (a translations file/map), never machine-translated
  silently.
- **Rename caveat:** target the current `publish` plugin (legacy text says "PRESSROOM").

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0023, PLAN_0028)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (locale edition localises prose + figure labels;
non-externalised labels → disclosed limitation, never a silently-untranslated figure), the test
coordinates, then the label-externalisation convention + per-locale render. Emit `STORY_PROVEN` +
`DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit
`.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue, and the `state`
column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; a non-externalised-label figure is disclosed rather than shipped
  untranslated; coverage of the localisation logic is 100%.
