# Add the self-improving description optimiser

## Metadata
| | |
| --- | --- |
| **Plan** | `0060` |
| **Epic** | `0054` |
| **Depends on** | — |

**Brief**: Run the skill-creator's description optimiser on a schedule, automatically sharpening the
`code-quality` skill's triggering accuracy based on usage patterns — gating any material triggering shift
for review.

## User stories
- AS the marketplace owner I WANT the code-quality skill's description sharpened automatically from usage
  SO THAT its triggering accuracy improves without manual tuning.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL be able to run the description optimiser on a schedule against
  observed usage patterns.
- `EARS-002` (Event-driven): WHEN the optimiser runs THE SYSTEM SHALL propose a sharpened skill description
  derived from trigger/usage data.
- `EARS-003` (Unwanted): IF an optimised description would broaden/narrow triggering beyond the skill's
  scope THEN THE SYSTEM SHALL surface the change for review rather than apply it blindly.

## Acceptance criteria
1. Given usage data, When the optimiser runs, Then a sharpened description is proposed with the rationale.
2. Given a proposed change, When it materially shifts triggering, Then it is gated for review.

## Human interface test plan
None — the optimiser is a scheduled job producing a proposal. Proof: fixture usage data yields a sharpened
description + rationale; a proposal that materially shifts triggering is gated for review rather than
auto-applied (asserted on the proposal + gate decision).

## Implementation notes
- Reuse the skill-creator's description optimiser; schedule it; feed trigger/usage signals. The
  material-shift gate (EARS-003) prevents blind scope-creep — surface for review, never auto-apply.
- Scheduling is exercised deterministically in tests (no real timer/wall-clock).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0054, PLAN_0060)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (proposal + rationale; material shift → gated for
review), the test coordinates (deterministic schedule), then the optimiser wiring + review gate. Emit
`STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT
`git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue,
and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; a material triggering shift is gated for review (never blindly applied);
  coverage of the optimiser + gate logic is 100%.
