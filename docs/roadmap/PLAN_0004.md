# Migrate the foundry / lifecycle / core backlog to v2

## Metadata
| | |
| --- | --- |
| **Plan** | `0004` |
| **Epic** | `0003` |
| **Depends on** | — |

**Brief**: decompose the foundry/lifecycle/core legacy backlog items into conformant v2 EPIC/PLAN docs.

## Legacy items in this batch
`.i2p/roadmap/backlog/`: 32 (foundry lifecycle delivery hardening — already EPIC-shaped), 34
(EARS/Gherkin/story proof for PR-56 — **a child of item 32**: emit it as a PLAN under item-32's EPIC,
not a separate top-level item), 35 (github-mcp setup installer), 43 (mcp-exposure policy + opportunity
scan), 44 (taxonomy reorg / value-handling paradigm), 46 (docs two-form completion).

## Construction process
Run roadmapper's **authoring value task** (`plugins/foundry/skills/roadmapper/SKILL.md` §3.5): for each
legacy item above, spawn `builder-lead` (authoring sub-cycle) + `handler-roadmap-decomposition` to carve
it into INVEST-sized v2 vertical slices, preserving each item's EARS/acceptance into the `PLAN_NNNN.md`
sections (§3.3). Item 32 is already an umbrella → emit it as an `EPIC_NNNN.md` with its children as
`## Plans`; the rest become PLAN(s) under an appropriate EPIC. Append the new EPIC rows to
`docs/roadmap/.pipeline.md`; claim global ids from `0010` up (after this EPIC's PLANs). Do NOT
`git push` or edit another batch's docs; do NOT delete the legacy files (they stay as history).

## Definition of done
- Each listed legacy item is represented by a conformant v2 doc (mapping recorded here on completion).
- `scripts/verify-prereqs.sh` check P GREEN; `.pipeline/verify` passes.

## Migration mapping (completed)
Each legacy item in this batch is now represented by a conformant v2 doc under `docs/roadmap/`, and its
EPIC row is appended to the `.pipeline.md` manifest (state `available`; the engine owns later state):

| legacy item | v2 doc | role |
| --- | --- | --- |
| 32 (foundry lifecycle delivery hardening — umbrella) | [EPIC_0010.md](./EPIC_0010.md) | EPIC (umbrella → `## Plans`) |
| 34 (EARS/Gherkin/story proof for PR #56 — child of 32) | [PLAN_0011.md](./PLAN_0011.md) | PLAN under EPIC_0010 |
| 35 (github-mcp setup installer) | [PLAN_0013.md](./PLAN_0013.md) | PLAN under EPIC_0012 |
| 43 (mcp-exposure policy + opportunity scan) | [PLAN_0014.md](./PLAN_0014.md) | PLAN under EPIC_0012 |
| 44 (taxonomy reorg / value-handling paradigm) | [PLAN_0016.md](./PLAN_0016.md) | PLAN under EPIC_0015 |
| 46 (docs two-form completion) | [PLAN_0017.md](./PLAN_0017.md) | PLAN under EPIC_0015 |

New EPICs emitted (ids claimed from `0010` up): `EPIC_0010` (foundry lifecycle delivery hardening),
`EPIC_0012` (govern the marketplace MCP surface — groups items 35 + 43), `EPIC_0015` (reorganise the
marketplace taxonomy & documentation — groups items 44 + 46). Ids `0010`–`0017` are consumed by this
batch; manifest rows for `0010`/`0012`/`0015` are appended to `docs/roadmap/.pipeline.md`.

**Provenance notes:**
- **Legacy item 33** (interactive *"Merge PR now?"*) was a child of the item-32 umbrella but has **no
  backlog file in this batch**; under the FLEET v2 pipeline the engine owns land + merge, so it is
  recorded as **superseded** (EPIC_0010 shared-infra caveat), not silently dropped.
- **Retired-`flow` provenance:** items 34 and 43 reference the `flow`/`flow-server` plugin, **retired**
  in commit `da07db8`. PLAN_0011 and PLAN_0014 preserve the legacy intent but instruct the builder to
  **re-target** the spec onto the current surface before building (caveats inline in each PLAN).
- Legacy `.i2p/roadmap/backlog/*` files are **kept as history** (not deleted), per the migration decision.
