# idea-to-production pipeline

> Execution manifest for idea-to-production's continuous delivery, in the FLEET pipeline standard
> (federation rollout). State lives HERE. Ascending by order; build the first `available` row. The
> marketplace plugins and the flow-server/MCP surface are intentionally kept; only the legacy
> roadmap-ticker is superseded by this pipeline.

| order | epic | state | constructs | branch |
| --- | --- | --- | --- | --- |
| `0000` | [0000_EPIC.md](./0000_EPIC.md) | `available` | a real CI gate | `pipeline/0000-ci-gate` |
