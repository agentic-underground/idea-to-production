# Add publish brand/style themes (publish.config.json)

## Metadata
| | |
| --- | --- |
| **Plan** | `0026` |
| **Epic** | `0023` |
| **Depends on** | — |

**Brief**: A project `publish.config.json` carrying fonts, colours, and a LaTeX/CSS theme so every artefact
(PDF, slides, web) shares one visual identity.

## User stories
- AS a builder with a brand I WANT one config that themes every publish output SO THAT my PDF, slides, and
  web articles look like one product, not three.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL read a project `publish.config.json` (fonts, colours, theme)
  and apply it consistently across every output format.
- `EARS-002` (Event-driven): WHEN any artefact is rendered THE SYSTEM SHALL apply the configured theme
  tokens (fonts/colours) to that format's renderer (CSS for web/slides, LaTeX preamble for PDF).
- `EARS-003` (Optional): WHERE no `publish.config.json` is present THE SYSTEM SHALL fall back to the
  built-in default theme.

## Acceptance criteria
1. Given a `publish.config.json` with a brand palette, When a PDF and a web article are rendered, Then both
   use the configured fonts and colours.
2. Given no config, When rendering, Then the default theme is used with no error.

## Human interface test plan
None — theming is applied in the render pipeline. Proof: a fixture config themes both a PDF and a web
render (tokens present in output); absent config falls back to default with no error.

## Implementation notes
- Define the config schema (fonts, colour roles, per-format theme hooks).
- Map tokens → CSS variables (web/slides) and → LaTeX preamble (PDF) — applies across the render path
  EPIC_0018's formats use.
- **Rename caveat:** the legacy item names the config `pressroom.config.json`; use **`publish.config.json`**
  to match the current plugin name (`publish`).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0023, PLAN_0026)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (palette applied to PDF+web; no-config → default,
no error), the test coordinates, then the config schema + token→CSS/LaTeX mapping. Emit `STORY_PROVEN` +
`DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit
`.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue, and the `state`
column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; the no-config default-theme fallback raises no error; coverage of the
  config-load + token-mapping logic is 100%.
