# Add the findings ledger (SECURITY-LEDGER.jsonl trends over time)

## Metadata
| | |
| --- | --- |
| **Plan** | `0038` |
| **Epic** | `0029` |
| **Depends on** | — |

**Brief**: A machine-readable `SECURITY-LEDGER.jsonl` accumulating gate verdicts over time, so trends
(regressions, recurring false positives) are visible and feed self-improvement.

## User stories
- AS a security owner I WANT each gate run appended to a ledger SO THAT I can see regressions and recurring
  false positives over time and feed them back into the rules.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL append each security-gate run's verdict + findings summary to
  a machine-readable `SECURITY-LEDGER.jsonl`.
- `EARS-002` (Event-driven): WHEN the security gate completes THE SYSTEM SHALL append one ledger record
  (timestamp, verdict, per-lens counts, new vs recurring findings).
- `EARS-003` (State-driven): WHILE a finding recurs across runs THE SYSTEM SHALL mark it recurring in the
  ledger so persistent false-positives are visible for rule tuning.

## Acceptance criteria
1. Given two gate runs, When the second completes, Then the ledger has two records and marks any finding
   present in both as recurring.
2. Given the ledger, When read, Then verdict and per-lens counts over time are reconstructable.

## Human interface test plan
None — the ledger is an append-only data file. Proof: two fixture gate runs produce two JSONL records, a
finding in both is marked recurring, and a reducer reconstructs verdict/per-lens trends (asserted on the
ledger contents).

## Implementation notes
- Append-only JSONL (one record per run); a small reducer for trend reporting. Timestamp is injected in
  tests for determinism (no wall-clock flakiness).
- Feeds the self-improvement covenant (recurring FPs → rule fixes).
- **Rename caveat:** the gate is `/security:scan-all` (legacy `/security-gate`).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0029, PLAN_0038)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (two runs → two records + recurring mark;
reconstructable trends), the test coordinates (injected timestamp — deterministic), then the append-only
ledger writer + recurring-finding detection + trend reducer. Emit `STORY_PROVEN` + `DELIVERY_COMPLETE`
keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit `.pipeline.md` / the
EPIC's `## Plans` table** — the engine owns sync, land, PR/issue, and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; recurring findings are marked across runs; trends are reconstructable;
  coverage of the ledger logic is 100% (deterministic timestamp — no flakiness).
