# Add stack-aware code-quality analysis (framework lenses)

## Metadata
| | |
| --- | --- |
| **Plan** | `0055` |
| **Epic** | `0054` |
| **Depends on** | — |

**Brief**: Auto-detect the framework (FastAPI, Django, Rails, Spring, …) and load a framework-specific lens
alongside the generic ones — e.g. FastAPI router organisation + DI patterns; Django fat-vs-thin models +
signals abuse; Rails concerns/service/form objects.

## User stories
- AS a developer on a framework project I WANT code-quality to apply framework-specific checks SO THAT it
  catches idiom-level issues the generic lenses miss.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL detect the project framework and apply a matching framework
  lens alongside the generic lenses.
- `EARS-002` (Event-driven): WHEN a FastAPI/Django/Rails (etc.) project is analysed THE SYSTEM SHALL apply
  that framework's specific checks (e.g. Django fat-vs-thin models, Rails service objects).
- `EARS-003` (Unwanted): IF the framework cannot be detected THEN THE SYSTEM SHALL fall back to the generic
  lenses only (no false framework-specific findings).

## Acceptance criteria
1. Given a FastAPI project, When analysed, Then router-organisation / DI-pattern findings are produced
   alongside the generic ones.
2. Given an unrecognised stack, When analysed, Then only generic lenses run.

## Human interface test plan
None — analysis is a skill invocation. Proof: a FastAPI fixture yields framework-specific findings + the
generic ones; an unrecognised stack runs generic-only with no false framework findings (asserted on the
findings set).

## Implementation notes
- Framework auto-detect; per-framework lens modules (FastAPI/Django/Rails/Spring to start) over the
  `code-quality` engine (`plugins/foundry/skills/code-quality/`).
- The undetected-framework path must not emit framework-specific findings (the abuse-path invariant).

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0054, PLAN_0055)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (framework lens applied; undetected → generic-only,
no false findings), the test coordinates, then the detection + per-framework lens modules. Emit
`STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT
`git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue,
and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; the undetected-framework path stays generic-only; coverage of the
  detection + lens logic is 100%.
