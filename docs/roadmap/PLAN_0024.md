# Add publish citations & bibliography (references.bib + cross-format notes)

## Metadata
| | |
| --- | --- |
| **Plan** | `0024` |
| **Epic** | `0023` |
| **Depends on** | — |

**Brief**: When an article references sources, manage a `references.bib` and render footnotes/endnotes and
a bibliography consistently across markdown, PDF, and HTML.

## User stories
- AS an author I WANT to cite sources once and have them rendered correctly in every output format SO THAT
  I don't hand-maintain footnotes per channel.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL manage a `references.bib` and render citations + a
  bibliography consistently across markdown, PDF, and HTML outputs.
- `EARS-002` (Event-driven): WHEN an article cites a key present in `references.bib` THE SYSTEM SHALL
  render the in-text citation and the corresponding footnote/endnote and bibliography entry in the active
  format's idiom.
- `EARS-003` (Unwanted): IF a cited key is missing from `references.bib` THEN THE SYSTEM SHALL surface the
  unresolved citation, and SHALL NEVER silently drop it or emit a dangling marker.

## Acceptance criteria
1. Given an article citing `[@key]` with `key` in `references.bib`, When rendered to PDF and HTML, Then
   both show a consistent in-text citation and a bibliography entry.
2. Given a citation whose key is absent, When rendered, Then the build reports the unresolved key.

## Human interface test plan
None — citation rendering is part of the render pipeline. Proof: a fixture article + `references.bib`
renders consistent citations to PDF and HTML; a missing key is reported (asserted on build output).

## Implementation notes
- Pandoc **citeproc** (or equivalent) for cross-format citation rendering — hooks the same render path
  the format matrix uses.
- Define where `references.bib` lives relative to the article source (convention documented).
- Declare any new engine in the publish `check` manifest, no-download.
- **Rename caveat:** target the current `publish` plugin (legacy text says "PRESSROOM").

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0023, PLAN_0024)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (resolved key renders consistently to PDF+HTML;
missing key reported, never dangling), the test coordinates, then the citeproc-backed citation rendering +
`references.bib` convention. Emit `STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD** and write
`IDEA_COST.jsonl`. **Do NOT `git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** — the engine
owns sync, land, PR/issue, and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (B/G for new probes, P) stays GREEN.
- Both acceptance criteria pass; the missing-key path reports rather than drops/dangles; coverage of the
  citation logic is 100%.
