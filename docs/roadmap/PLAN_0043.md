# Automate adversarial design-critic spawning (scored loop)

## Metadata
| | |
| --- | --- |
| **Plan** | `0043` |
| **Epic** | `0039` |
| **Depends on** | — |

**Brief**: The design system documents *how* to run the design-critic and its definition-of-good. This
slice automates spawning critic sub-agents on every build and routes their findings (and the
Feedback-Marker signal) into a scored, tracked improvement loop — the second-way HIL optimisation closing
on itself.

## User stories
- AS a builder I WANT the design-critic to run automatically on every build SO THAT design feedback is
  continuous and tracked, not a manual step I forget.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL be able to spawn the adversarial design-critic automatically
  and route its findings into a scored, tracked improvement loop.
- `EARS-002` (Event-driven): WHEN a build completes THE SYSTEM SHALL spawn critic sub-agent(s), collect
  their findings + Feedback-Marker signal, score them, and record the result in the improvement loop.
- `EARS-003` (Unwanted): IF a critic sub-agent cannot run THEN THE SYSTEM SHALL surface that the design
  lens did not run rather than record a clean pass (no false-green).

## Acceptance criteria
1. Given a build, When it completes, Then critic sub-agents run automatically and their scored findings are
   recorded.
2. Given a critic that fails to run, When the build finishes, Then the gap is disclosed (no false-green).

## Human interface test plan
None — this automates an agent-orchestration loop, not a UI element. Proof: a fixture build triggers the
critic spawn, scored findings are recorded in the loop, and a forced critic-failure surfaces the gap
rather than a clean pass (asserted on the loop record, with the critic spawn mocked — deterministic).

## Implementation notes
- Build on the design system's documented design-critic procedure + definition-of-good
  (`plugins/foundry/skills/frontend/`), composing the `atelier` design-reviewer lens where present.
- Wire findings + the Feedback-Marker into a scored, tracked loop (the second-way HIL). The
  critic-cannot-run path must disclose the gap (mirrors check-style fail-loud, not a vacuous pass).
- Tests mock the critic spawn — no live sub-agent — so they stay deterministic.

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0039, PLAN_0043)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (auto-spawn + scored recording; critic-fails →
disclosed gap, never false-green), the test coordinates (mocked critic), then the auto-spawn + scoring +
loop-recording. Emit `STORY_PROVEN` + `DELIVERY_COMPLETE` keyed to **branch HEAD** and write
`IDEA_COST.jsonl`. **Do NOT `git push` or edit `.pipeline.md` / the EPIC's `## Plans` table** — the engine
owns sync, land, PR/issue, and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; a critic that cannot run discloses the gap (no false-green); coverage of
  the spawn + scoring logic is 100% (critic mocked — no flakiness).
