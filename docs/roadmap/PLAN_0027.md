# Add publish diagram round-tripping (import & re-compose to the charting matrix)

## Metadata
| | |
| --- | --- |
| **Plan** | `0027` |
| **Epic** | `0023` |
| **Depends on** | — |

**Brief**: Import existing Mermaid / DOT / draw.io diagrams and re-compose them to the 4×9 charting matrix
(dark-mode, transparent, the legibility lessons), flagging legibility violations for fix.

## User stories
- AS an author with legacy diagrams I WANT to import them and have publish re-style them to the charting
  matrix SO THAT they match the rest of my figures and pass the legibility bar.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL import Mermaid, DOT, and draw.io sources and re-render them to
  the publish charting matrix (dark-mode, transparent, the 4×9 lessons).
- `EARS-002` (Event-driven): WHEN a diagram is imported THE SYSTEM SHALL re-compose it to the matrix and
  report any legibility violations (contrast, label overlap, encoding) for the author to fix.
- `EARS-003` (Unwanted): IF a source uses a construct the matrix cannot express THEN THE SYSTEM SHALL
  disclose the lossy mapping rather than silently dropping nodes/edges.

## Acceptance criteria
1. Given a Mermaid/DOT/draw.io file, When imported, Then a matrix-styled dark-mode figure is produced.
2. Given a diagram with a legibility violation, When re-composed, Then the violation is flagged with its
   location.

## Human interface test plan
None — import/re-compose is a render-pipeline operation. Proof: fixture Mermaid/DOT/draw.io files re-render
to matrix-styled dark-mode figures; a planted legibility violation is flagged with its location; a
matrix-inexpressible construct discloses the lossy mapping.

## Implementation notes
- Parse each source into a **common intermediate**, then drive the existing charting-matrix handlers
  (`diagram-studio`/`illustrator`) — the shared figure pipeline (see EPIC_0023 shared-infra).
- Reuse the `design-reviewer`'s legibility checks for the violation report.
- Respect the figure **label-externalisation convention** shared with PLAN_0028 (localisation).
- **Rename caveat:** target the current `publish` plugin (legacy text says "PRESSROOM").

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0023, PLAN_0027)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (each source → matrix figure; violation flagged
with location; inexpressible construct → disclosed lossy mapping, never silent drop), the test
coordinates, then the importer→intermediate→matrix-handler path + legibility report. Emit `STORY_PROVEN` +
`DELIVERY_COMPLETE` keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit
`.pipeline.md` / the EPIC's `## Plans` table** — the engine owns sync, land, PR/issue, and the `state`
column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (B/G for any new probes, P) stays GREEN.
- Both acceptance criteria pass; the inexpressible-construct path discloses the lossy mapping rather than
  dropping nodes/edges; coverage of the import + re-compose logic is 100%.
