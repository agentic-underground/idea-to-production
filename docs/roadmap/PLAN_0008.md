# Migrate the comfyui-mcp backlog to v2

## Metadata
| | |
| --- | --- |
| **Plan** | `0008` |
| **Epic** | `0003` |
| **Depends on** | — |

**Brief**: migrate the comfyui-mcp legacy backlog — already an EPIC→children shape — into v2 form.

## Legacy items in this batch
`.i2p/roadmap/backlog/`: 73 (comfyui-mcp secured backend — the legacy EPIC), 74 (server build),
75 (enforcement core), 76 (boundary hardening), 77 (container), 78 (gate/docs/registration),
79 (phase2 template UIs), 80 (phase2 pool & cache), and 49 (comfyui-mcp secured-backend adoption).

## Construction process
This batch is **already umbrella-shaped** (73 is the EPIC; 74–80 its children; 49 the adoption item) —
the cleanest legacy→v2 mapping in the migration. Run roadmapper's **authoring value task** (§3.5):
emit item 73 as an `EPIC_NNNN.md` whose `## Plans` are 74–80 (each a `PLAN_NNNN.md` carrying that
child's EARS/acceptance), with the shared-infra map (the comfyui-mcp server + container underpin the
phase-2 items) and the dependency order (build → enforcement → boundary → container → gate → phase-2).
Fold 49 (adoption) in as its own PLAN or the EPIC's final slice. Append the EPIC row to `.pipeline.md`;
claim global ids from the shared counter. No push; keep legacy files.

## Definition of done
- Each listed legacy item is represented by a conformant v2 doc (mapping recorded here on completion).
- `scripts/verify-prereqs.sh` check P GREEN; `.pipeline/verify` passes.

## Migration mapping (completed)
The comfyui-mcp batch (already umbrella-shaped) is migrated into one conformant v2 EPIC under
`docs/roadmap/` — [EPIC_0045.md](./EPIC_0045.md) ("build the comfyui-mcp secured generative backend") —
ids claimed from the shared counter (`0045`–`0053`, after PLAN_0007's `0039`–`0044`), with the EPIC row
appended to the `.pipeline.md` manifest (state `available`). PLANs are in dependency order:

| legacy item | v2 doc | order |
| --- | --- | --- |
| 73 (comfyui-mcp secured-backend — the legacy EPIC) | [EPIC_0045.md](./EPIC_0045.md) | EPIC |
| 74 (server build, 5 MCP tools) | [PLAN_0046.md](./PLAN_0046.md) | 0000 (buildable core) |
| 75 (enforcement core) | [PLAN_0047.md](./PLAN_0047.md) | 0001 |
| 76 (boundary hardening) | [PLAN_0048.md](./PLAN_0048.md) | 0002 |
| 77 (container) | [PLAN_0049.md](./PLAN_0049.md) | 0003 |
| 78 (gate / docs / registration) | [PLAN_0050.md](./PLAN_0050.md) | 0004 |
| 49 (publish adoption — handler-comfyui curl→MCP) | [PLAN_0051.md](./PLAN_0051.md) | 0005 |
| 79 (Phase 2 — per-template UIs / presets) | [PLAN_0052.md](./PLAN_0052.md) | 0006 |
| 80 (Phase 2 — worker pool + result cache) | [PLAN_0053.md](./PLAN_0053.md) | 0007 |

Ids `0045`–`0053` consumed; manifest row `0045` appended. The dependency order (build → enforcement/
boundary/container → gate → adoption; Phase-2 after the core) is preserved as PLAN-level `depends_on`.

**Provenance notes:**
- **Renames.** Item 78's gate is now the `security` plugin (`/security:scan-all`, `/security:scan-for-secrets`,
  `/security:scan-dependencies`) — legacy names SENTINEL. Item 49's handler is now
  `plugins/publish/agents/handler-comfyui.md` — legacy names PRESSROOM `plugins/pressroom/`. Each carries
  the caveat inline.
- **Retired-`flow` reference.** Item 78 cites the "flow-server registration discipline"; the `flow` plugin
  is retired. PLAN_0050 follows the **current** registration discipline that `verify-prereqs` checks C
  (shipped ⟺ `40-mcp.md` parity) and K (launcher version-pin) enforce.
- Legacy `.i2p/roadmap/backlog/*` files are **kept as history** (not deleted), per the migration decision.
