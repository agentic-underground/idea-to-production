# Migrate the security backlog to v2

## Metadata
| | |
| --- | --- |
| **Plan** | `0006` |
| **Epic** | `0003` |
| **Depends on** | — |

**Brief**: decompose the security-plugin legacy backlog items into conformant v2 EPIC/PLAN docs.

## Legacy items in this batch
`.i2p/roadmap/backlog/`: 59 (license-audit skill), 60 (SBOM skill), 61 (unified `.securityignore`),
62 (severity-policy config), 63 (CI-integration recipes), 64 (container & IaC scanning), 65 (diff-scoped
mode), 66 (live credential verification), 67 (findings ledger).

## Construction process
Run roadmapper's **authoring value task** (§3.5): carve these into v2 docs, likely one "security scan
surface" EPIC with PLAN-per-capability plus a shared-infra map (they share the scan-report + ignore/
severity config). Use `handler-roadmap-decomposition` for INVEST slices + dependency order (e.g.
severity-policy/`.securityignore` underpin the scanners). Preserve each item's EARS/acceptance into the
`PLAN_NNNN.md` sections. Append the new EPIC rows to `.pipeline.md`; claim global ids from the shared
counter. No push; keep legacy files.

## Definition of done
- Each listed legacy item is represented by a conformant v2 doc (mapping recorded here on completion).
- `scripts/verify-prereqs.sh` check P GREEN; `.pipeline/verify` passes.
