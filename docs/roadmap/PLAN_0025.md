# Add publish changelog & release-notes automation

## Metadata
| | |
| --- | --- |
| **Plan** | `0025` |
| **Epic** | `0023` |
| **Depends on** | — |

**Brief**: Generate release notes directly from conventional commits between two refs, then let the writer
polish them — a `/publish:publish release-notes vX..vY` flow.

## User stories
- AS a maintainer I WANT release notes generated from the commits between two tags SO THAT I don't
  hand-assemble them; AND AS an editor I WANT the writer to polish the draft SO THAT they read well.

## EARS specification
- `EARS-001` (Ubiquitous): THE SYSTEM SHALL generate release notes from the conventional-commit history
  between two git refs and pass the draft through the writer for polish.
- `EARS-002` (Event-driven): WHEN `/publish:publish release-notes vX..vY` runs THE SYSTEM SHALL collect
  commits in that range, group them by conventional-commit type (feat/fix/docs/…), and emit a polished,
  human-readable changelog.
- `EARS-003` (Unwanted): IF the range contains non-conventional commit subjects THEN THE SYSTEM SHALL
  still include them under an "other" group rather than dropping them.

## Acceptance criteria
1. Given two refs, When the flow runs, Then a changelog grouped by commit type is produced for exactly the
   commits in that range.
2. Given the generated draft, When the writer polishes it, Then the output is readable prose, not a raw
   commit dump.

## Human interface test plan
None — `release-notes vX..vY` is a CLI invocation. Proof: against a fixture git range, the grouped changelog
covers exactly that range and non-conventional subjects land under "other" (asserted on output).

## Implementation notes
- Parse the conventional-commit history **directly from git** for the given `vX..vY` range. **Re-target
  caveat:** the legacy note suggests reusing "the flow-server's git-log synthesis" — the `flow` plugin is
  **retired** (commit `da07db8`); use `git log` directly, not the retired flow-server.
- Wire `release-notes` as a `/publish:publish` mode taking a `vX..vY` range; route the draft through the
  writer skill for polish.
- **Rename caveat:** target the current `publish` plugin (legacy text says "PRESSROOM").

## Construction process
Build THIS plan by invoking FOUNDRY scoped to `(EPIC_0023, PLAN_0025)`: drive DEV_SYSTEM M0→M6 to a GREEN
tree **on this branch** — author the EARS, the Gherkin (grouped changelog over exactly the range;
non-conventional → "other"; writer polish → prose not a dump), the test coordinates (fixture git range),
then the range-collection + grouping + writer-polish wiring. Emit `STORY_PROVEN` + `DELIVERY_COMPLETE`
keyed to **branch HEAD** and write `IDEA_COST.jsonl`. **Do NOT `git push` or edit `.pipeline.md` / the
EPIC's `## Plans` table** — the engine owns sync, land, PR/issue, and the `state` column.

## Definition of done
- `.pipeline/verify` is GREEN; `scripts/verify-prereqs.sh` (P) stays GREEN.
- Both acceptance criteria pass; non-conventional subjects are retained under "other", never dropped;
  coverage of the collection + grouping logic is 100%.
